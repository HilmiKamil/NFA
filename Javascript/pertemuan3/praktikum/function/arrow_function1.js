// function expression

const sapa = function sapa(nama){
    return `Halo, ${nama} !`;
}

// Arrow expression

const sapaArrow = (nama) =>{
    return `Halo, ${nama} !`;
}

console.log(sapaArrow("Andi"));